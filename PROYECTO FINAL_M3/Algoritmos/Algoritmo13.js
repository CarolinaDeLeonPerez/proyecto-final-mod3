console.log(`${algoritmo} 13`);

let total = 0;
let cifras = [1,1,1,1];

//Definimos los valores que darán pie al programa. "cifras" para los valores que se sumaran y "total" para ubicar el resultado acumulado.

for (let i = 0; i < cifras.length; i++) total+=cifras[i];
console.log(total); //4
